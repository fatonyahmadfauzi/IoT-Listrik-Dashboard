# Copy this file to `keystore.properties` after generating the keystore.
#
# Keystore signing for Android `release` builds.
#
# Jangan commit file `keystore.properties` karena berisi password.

storeFile=release-keystore.jks
storePassword=CHANGE_ME_STORE_PASSWORD
keyAlias=fatony
keyPassword=CHANGE_ME_KEY_PASSWORD

